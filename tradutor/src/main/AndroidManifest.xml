<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- notificacao fixa com o atalho de traduzir -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tradutor">
        <!-- o modelo do OCR vem junto com o app, para a primeira leitura nao esperar download -->
        <meta-data android:name="com.google.mlkit.vision.DEPENDENCIES" android:value="ocr" />
        <!-- necessario para o instalador abrir o APK que o atualizador baixou -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.arquivos"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/caminhos" />
        </provider>
        <activity android:name=".MainActivity" android:exported="true" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Leitor de capitulo: recebe o endereco compartilhado do navegador. So ACTION_SEND: com ACTION_VIEW
             o app viraria candidato a abrir todo link do sistema, que nao e o que o dono quer. -->
        <activity android:name=".LeitorActivity" android:exported="true" android:screenOrientation="portrait"
            android:label="@string/leitor_nome">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        <!-- A captura da tela e a bolha vivem aqui. Servico de acessibilidade pode criar janela do tipo
             TYPE_ACCESSIBILITY_OVERLAY sem a permissao de desenhar sobre outros apps: uma permissao a menos. -->
        <service
            android:name=".ServicoTradutor"
            android:exported="true"
            android:label="@string/servico_nome"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data android:name="android.accessibilityservice" android:resource="@xml/servico" />
        </service>
    </application>
</manifest>
